from setuptools import setup

setup(
   name='static_site',
   version='0.0',
   description='A python module for generating a static html website.',
   author='Fletcher Graham',
   author_email='sfletchgraham@gmail.com',
   packages=['static_site'],
   install_requires=['markdown'],
)
